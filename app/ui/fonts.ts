import { Inter, Geist_Mono, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const geistMono = Geist_Mono({ subsets: ['latin'] });

export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: ['700', '400'],
});
